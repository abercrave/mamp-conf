#
# Virtual Hosts
#
# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at 
# <URL:http://httpd.apache.org/docs/2.2/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

#
# Use name-based virtual hosting.
#
NameVirtualHost *:80
NameVirtualHost *:443

#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#

# Drupal overrides
<VirtualHost *:80>
	ServerName website.ultraviolet.digi
	DocumentRoot /www/htdocs/ultraviolet/website/drupal
</VirtualHost>
<VirtualHost *:80>
	ServerName website.princeton.digi
	DocumentRoot /www/htdocs/princeton/website/drupal
</VirtualHost>
##
# Dynamic virtual hostging
<VirtualHost *:80>
	ServerName localhost
	ServerAlias *.digi

	RewriteEngine on
	RewriteMap lowercase int:tolower
	
	# Project level subdomains
	RewriteCond %{REQUEST_URI} !^/icons/
	RewriteCond ${lowercase:%{SERVER_NAME}} ^(?:[a-z0-9\-]+\.){2}dev$
	RewriteRule ^(.+) ${lowercase:%{SERVER_NAME}}$1 [C]
	RewriteRule ^(.*)\.(.*)\.digitaria\.digi/(.*) /$2/$1/$3 [L]

	# Client level subdomains
	RewriteCond %{REQUEST_URI} !^/icons/
	RewriteCond ${lowercase:%{SERVER_NAME}} ^[a-z0-9\-]+\.digi$
	RewriteRule ^(.+) ${lowercase:%{SERVER_NAME}}$1 [C]
	RewriteRule ^(.*)\.digitaria\.digi/(.*) /$1/$2
</VirtualHost>
