#
# Virtual Hosts
#
# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at 
# <URL:http://httpd.apache.org/docs/2.2/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

#
# Use name-based virtual hosting.
#
NameVirtualHost *:80
NameVirtualHost *:443

#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#

<VirtualHost *:80>
	ServerName localhost
	ServerAlias *.digitaria.local

	RewriteEngine on
	RewriteMap lowercase int:tolower
	
	## deal with normal documents first:
	# allow Alias /icons/ to work - repeat for other aliases
	RewriteCond %{REQUEST_URI} !^/icons/
	# check the hostname is right so that the RewriteRule works
	RewriteCond ${lowercase:%{SERVER_NAME}} ^(?:[a-z0-9\-]+\.){2}digitaria\.local$
	# concatenate the virtual host name onto the start of the URI
	# the [C] means do the next rewrite on the result of this one
	RewriteRule ^(.+) ${lowercase:%{SERVER_NAME}}$1 [C]
	# now create the real file name
	RewriteRule ^(.*)\.(.*)\.digitaria\.local/(.*) /$2/$1/$3 [L]
	
	## deal with normal documents first:
	# allow Alias /icons/ to work - repeat for other aliases
	RewriteCond %{REQUEST_URI} !^/icons/
	# check the hostname is right so that the RewriteRule works
	RewriteCond ${lowercase:%{SERVER_NAME}} ^[a-z0-9\-]+\.digitaria\.local$
	# concatenate the virtual host name onto the start of the URI
	# the [C] means do the next rewrite on the result of this one
	RewriteRule ^(.+) ${lowercase:%{SERVER_NAME}}$1 [C]
	# now create the real file name
	RewriteRule ^(.*)\.digitaria\.local/(.*) /$1/$2
</VirtualHost>

# <VirtualHost *:80>
#     ServerName localhost
#     DocumentRoot "/www/htdocs"
# </VirtualHost>
# 
# <VirtualHost *:80>
#     ServerName wkkf.digitaria.local
#     DocumentRoot "/www/htdocs/wkkf/Digitaria.Web.Wkkf2013.UI"
# </VirtualHost>
# 
# <VirtualHost *:80>
#     ServerName uvvu.digitaria.local
#     DocumentRoot "/www/htdocs/ultraviolet/website/docroot"
# </VirtualHost>
# 
# <VirtualHost *:80>
#     ServerName vbb.digitaria.local
#     DocumentRoot "/www/htdocs/nestle/verybestbaking.com/trunk/src/html"
# 	<Directory /www/htdocs/nestle/verybestbaking.com/trunk/src/html>
# 	    Options +Includes
# 	</Directory>
# 	Alias /Assets /www/htdocs/nestle/verybestbaking.com/trunk/src/aspnet/Nestle.VeryBestBaking.Web/Assets
# 	<Directory /www/htdocs/nestle/verybestbaking.com/trunk/src/aspnet/Nestle.VeryBestBaking.Web/Assets>
# 		Order allow,deny
# 		Allow from all
# 	</Directory>
# </VirtualHost>
# 
# <VirtualHost *:80>
#     ServerName tpr0.digitaria.local
#     DocumentRoot "/www/htdocs/tpr/phase0"
# </VirtualHost>